"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getJSONDataFromRequestStream = void 0;
const getJSONDataFromRequestStream = (request) => new Promise((resolve) => {
    const chunks = [];
    request.on('data', (chunk) => {
        chunks.push(chunk);
    });
    request.on('end', () => {
        try {
            resolve(JSON.parse(Buffer.concat(chunks).toString()));
        }
        catch (err) {
            resolve(JSON.parse('{}'));
        }
    });
});
exports.getJSONDataFromRequestStream = getJSONDataFromRequestStream;
//# sourceMappingURL=body-json.js.map