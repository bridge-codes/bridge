"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getJSONQueryFromURL = void 0;
const getJSONQueryFromURL = (queryUrl) => {
    const queryJSON = {};
    try {
        if (!queryUrl)
            return queryJSON;
        const queries = queryUrl.replace('?', '&').split('&');
        queries.forEach((query) => {
            const [key, value] = query.split('=');
            queryJSON[key] = value;
        });
        return queryJSON;
    }
    catch (err) {
        console.error(err);
        return queryJSON;
    }
};
exports.getJSONQueryFromURL = getJSONQueryFromURL;
//# sourceMappingURL=query-json.js.map